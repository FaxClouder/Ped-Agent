# Contracts

本目录只保存三个业务板块之间共享的稳定数据契约，不保存算法实现。

计划按职责拆分为：

- `extraction.py`：视频输入、检测配置和像素轨迹；
- `processing.py`：投影配置、优化配置和分析就绪轨迹；
- `analysis.py`：分析请求、指标结果和分析产物；
- `quality.py`：质量状态、使用限制、能力预检和跳过原因。

当前具体模型仍位于兼容实现中，后续迁移时再逐项建立，避免在目录阶段冻结不成熟字段。
